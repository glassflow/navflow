{
  "mcpServers": {
    "navflow": {
      "command": "navflow-mcp",
      "env": {
        "NAVFLOWD_URL": "${user_config.navflow_url}",
        "NAVFLOW_AUTH_TOKEN": "${user_config.access_token}"
      }
    }
  }
}
