{
  "mcpServers": {
    "navflow": {
      "command": "navflow-mcp",
      "env": {
        "NAVFLOWD_URL": "${user_config.navflow_url}",
        "NAVFLOW_AUTH_TOKEN": "${user_config.ingest_token}"
      }
    }
  }
}
