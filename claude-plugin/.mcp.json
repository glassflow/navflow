{
  "mcpServers": {
    "tares": {
      "command": "tares-mcp",
      "env": {
        "TARESD_URL": "${user_config.tares_url}",
        "TARES_AUTH_TOKEN": "${user_config.access_token}"
      }
    }
  }
}
